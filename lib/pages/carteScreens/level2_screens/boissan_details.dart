import 'package:flutter/material.dart';

class BoissanDetails extends StatelessWidget {
  const BoissanDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
