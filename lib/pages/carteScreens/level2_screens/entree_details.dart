import 'package:flutter/material.dart';

class EntreeDetails extends StatelessWidget {
  const EntreeDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
