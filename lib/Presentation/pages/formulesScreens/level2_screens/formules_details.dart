import 'package:flutter/material.dart';

class FormulesDetails extends StatelessWidget {
  const FormulesDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
